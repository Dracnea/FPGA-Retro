# Vivado / LiteX build output
build/
*.jou
*.log
.Xil/
__pycache__/
*.pyc

# Build output is not tracked -- except the images promoted into bitstreams/,
# which are kept so a card can be brought up without a Vivado seat.
*.bit
!bitstreams/*.bit
*.mcs
*.ltx
csr.csv
analyzer.csv
build_id.v
build_id.vh

venv/

# compiled test tools
tools/frametest/frametest
