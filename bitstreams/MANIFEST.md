# Bitstreams

Built images, kept in the repo so a card can be brought up without a Vivado
seat. **The md5 is the identity, not the filename** — record it and check it.

| file | md5 | part | built from | verified |
|---|---|---|---|---|
| `c1100_pcie_video_transport.bit` | `d156441c615a9d5199d2da4ebcba1f2d` | xcu55n-fsvh2892-2LV-e | `overlay/mistex_boards/c1100_pcie_video.py` — LitePCIe gen3 x4 endpoint, DMA with scatter-gather, `(frame << 24) \| pixel_index` frame source, `hbm_cattrip` low | **On hardware 2026-09-05:** enumerates as `10ee:9034`, BAR0 128 KiB, link trained 8.0 GT/s × 4, MSI bound, `litepcie` driver attached. On 2026-09-07 every image built from it read `0xffffffff` from every CSR until BAR0 was made 64-bit prefetchable (the 32-bit BAR landed in a host window the firmware never routed; `docs/c1100-pcie-transport.md`). Rebuilt that way (WNS +0.721 ns): scratch, identifier, DMA writer, MSI and `tools/frametest` (64 MiB, sequence intact within every buffer, ~200 µs enable-to-first-word) verified the same day. |
| `fk33_platform_smoke.bit` | `e2a0fd0745c7c516bfc650d6d69ee740` | xcvu33p-fsvh2104-2-e | `overlay/mistex_boards/sqrl_fk33_mistex.py smoke` — CRG from the 200 MHz oscillator plus a kept counter on the LEDs | Builds and closes timing; **no FK33 is attached to this host**, so unverified on silicon. |

| `c1100_ps2_iop.bit` | `3ac569521c495e99049842f3307e54ef` | xcu55n-fsvh2892-2LV-e | `overlay/mistex_boards/c1100_ps2_iop.py` — the same PCIe endpoint plus the PS2 IOP subsystem (`overlay/cores/PS2`: CPU, RAM/ROM, INTC, timers, SPU2 as two PSX SPUs, SIO2 with a pad on `iop_pad0`, CDVD with no disc) on CSRs; its `csr.csv` is `c1100_ps2_iop.csr.csv` here, which `tools/ps2iop/iop_post.py` needs | Builds and closes timing: WNS +0.168 ns, 98,932 endpoints, 0 failing; 20,791 LUTs, 82 BRAM, 224 URAM. **PASS on hardware 2026-09-07 22:04** (rebuilt with BAR0 64-bit prefetchable): `boot_test.s` reaches POST AA in 10 ms with all twelve stage writes, the pad-0xFFFF run fails at stage 09 as designed, five repeats pass (`docs/ps2-iop-bringup.md`). |
| `c1100_hps_test.bit` | `141da0e990f7d74ff4e0e2e0610e007b` | xcu55n-fsvh2892-2LV-e | `overlay/mistex_boards/c1100_hps_test.py` — the same PCIe endpoint plus the `hps_pcie` bridge driving a real `hps_io.sv` (config string `HPSTEST;;...`) in a 50 MHz core domain; `csr.csv` beside it is what `host/main_mistex_pcie` reads | Rebuilt 2026-09-07 with BAR0 64-bit prefetchable: WNS +0.731 ns, 0 failing endpoints; driver in `build/c1100_hps_test/software`. **Not loaded** — `c1100_hps_video_test.bit` covers the same HPS test and is verified. Expected first result with Main: `got: 'HPSTEST;;O1,Option one,...'` — `docs/host-gui-compatibility.md`. |
| `c1100_hps_video_test.bit` | `074562fc4f493161d0710d533effa420` | xcu55n-fsvh2892-2LV-e | `overlay/mistex_boards/c1100_hps_video_test.py` — `c1100_hps_test` plus a 640x480 pattern generator through `rtl/video_sink` into DMA0 as a FRM1 stream; `csr.csv` beside it serves both `host/main_mistex_pcie` and `host/viewer` | Builds and closes timing: WNS +0.598 ns, 31,213 endpoints, 0 failing; 5,214 LUTs. **Not loaded** (card busy 2026-09-06). **Verified on hardware 2026-09-07 22:14** (rebuilt with BAR0 64-bit prefetchable, driven with its own `build/c1100_hps_video_test/software` driver): 600 frames of 640x480 in 10 s at 59.5 fps through the sink and DMA ring, 0 dropped, 0 resyncs, 738 MB moved; contact sheet and AVI in `build/video/20260907-221426` (`docs/host-video-path.md`). Supersedes `c1100_hps_test.bit` for the HPS test too. |

All C1100 images from 2026-09-07 21:00 on declare BAR0 **64-bit prefetchable**; the earlier ones had a 32-bit BAR that this host places in a window its firmware never routed, so every register read returned `0xffffffff` (`docs/c1100-pcie-transport.md`).

Loading the C1100 image: JTAG (`program_hw_devices` from Vivado, or `fjtag
--load`), then `sudo tools/pcie-bringup.sh` to drop the stale PCIe identity and
rescan. A card that held a bitstream with no PCIe endpoint needs that rescan (or
a warm reboot) before the host sees the new device.

Not here: the out-of-context core fits in `build/fit_*` produce checkpoints, not
bitstreams — a core becomes a bitstream only once it sits in a board target with
the video sink and host interface (`docs/retro-cores.md`).
| `c1100_pcie_diag.bit` | `ada9c12f211eea804f4e5b8e045dd598` | xcu55n-fsvh2892-2LV-e | `overlay/mistex_boards/c1100_pcie_diag.py` — the transport design plus UARTbone on FPGA UART 0 (BJ41/BK41, shows up as `/dev/ttyUSB2`, 115200) two LiteScope analyzers (`zanalyzer_pcie.csv`, `zanalyzer_sys.csv` beside it) and the hard block's own status outputs as `zhardip_*` CSRs (`tools/pcie-hardip.py`); **BAR0 64-bit prefetchable** (the transport image's is 32-bit); pcie_* CSR addresses identical to the transport image | Built 2026-09-07: WNS −0.006 ns on two paths inside the pcie-domain analyzer's own trigger memory, nothing in the design under test. **Loaded the same day:** identifier, scratch write/read, PHY link status and both analyzers all work over the UART (`tools/uart-probe.sh`, `tools/pcie-scope.py`); `docs/c1100-pcie-transport.md`. |
| `c1100_ps2_diag.bit` | `43545949277f48ce1463be7ea31b213f` | xcu55n-fsvh2892-2LV-e | `overlay/mistex_boards/c1100_ps2_diag.py` — `c1100_ps2_iop` plus UARTbone (`/dev/ttyUSB2`, 115200), a 64-entry POST ring with IOP cycle stamps, a CPU-bus stall detector, the IOP serial console as a FIFO, and a LiteScope on the CPU's memory bus (`zanalyzer_iop.csv` beside it); the plain image's CSR addresses are unchanged | Built 2026-09-08: WNS +0.390 ns, 0 failing endpoints, no inert-constraint signature. For booting a real BIOS: `sudo tools/ps2iop/bios-hw.sh <rom0.bin>` (`docs/ps2-iop-bringup.md`). |
